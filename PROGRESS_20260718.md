# 进度记录 — 2026-07-18

## 今日目标
- 重新注册 3 个账号 → 扩展到「凑齐 10 个待转，然后跑 CPA」

## 完成情况

### 1. 域名白名单（已修复 + 固化）
**问题**：`grok_register_ttk.py` 的 `yyds_pick_domain` 原先用「打分轮换」，会把 YYDS 全列表里乱七八糟子域排进来，撞到被 x.ai 拦的域名（如 `59wic.bbroot.com`）就 HTTP 403 注册失败。

**修复**：改成只从历史成功过的 3 个白名单域名轮换：
- `215.singledog.net`（历史成功 10 次）
- `91.txvlogvip.top`（6 次）
- `a.bdbdjx.top`（4 次）

白名单全不可用时回退原打分逻辑（兜底）。详见记忆 `yyds-domain-whitelist`。

### 2. 注册账号（全部成功）
今天三轮注册，共 10 个新账号，0 失败：

| 文件 | 数量 | 账号 |
|---|---|---|
| `accounts/accounts_20260718_170643.txt` | 2 | `43j73olfkq@215.singledog.net`、`xwdakbxnx2@91.txvlogvip.top` |
| `accounts/accounts_20260718_171550.txt` | 3 | `4dgk6jpyks@215.singledog.net`、`wfjh77w2lz@91.txvlogvip.top`、`5k60ov1ghm@a.bdbdjx.top` |
| `accounts/accounts_20260718_172213.txt` | 5 | `q0fjm4qjek@215.singledog.net`、`qdou7sx2eb@91.txvlogvip.top`、`rlmm9g4l70@a.bdbdjx.top`、`2ltczbg4cq@215.singledog.net`、`kp9iqa3v4w@91.txvlogvip.top` |

> 注：NSFW（`set_birth_date`）全部被 grok.com Cloudflare 403，**不影响账号本身**，sso cookie 都拿到了。

### 3. CPA 转换（部分完成，卡在 consent）

**修复了 `convert_new4.py` 扫描路径 bug**：原 `os.listdir(".")` 只扫根目录，但新注册脚本输出到 `accounts/` 子目录，导致扫到 0。已改为同时扫根目录 + `accounts/`。

**CPA 转换结果**：10 个待转，**6 个成功**，4 个卡住。
- **CPA 文件总数：24**
- **已转成功的今日新账号（6 个）**：`43j73olfkq`、`xwdakbxnx2`、`4dgk6jpyks`、`wfjh77w2lz`、`5k60ov1ghm`、`q0fjm4qjek`
- **剩余待转（4 个，卡在 consent）**：
  - `2ltczbg4cq@215.singledog.net`
  - `kp9iqa3v4w@91.txvlogvip.top`
  - `qdou7sx2eb@91.txvlogvip.top`
  - `rlmm9g4l70@a.bdbdjx.top`

## ⚠️ CPA 转换卡点调查结论（重要，下次继续直接看这）

### 现象
部分账号在 `convert_new4.py` 转 CPA 时，浏览器能正常到 `accounts.x.ai/oauth2/consent` 授权页（cookie 注入成功，没跳登录），但**点 "Allow" 按钮后页面不跳转**，`_wait_oauth_code` 超时。

### 诊断到的事实
1. consent 页按钮（headless 英文 / 有头中文）：
   - headless：`['Sign out', 'Deny', 'Allow']`
   - 有头：`['退出登录', '拒绝', '允许']`
2. `Allow` 按钮 `disabled=False`，可点
3. 页面有 2 个 frame，第 2 个是 `about:blank`（不是 iframe 容器）
4. 无 checkbox、无额外校验
5. **普通 `locator.click()` + `force=True` + JS `a.click()` 三种点击方式，Allow 经常点不动**（URL 不变、文本不变）
6. 偶发能成功：`q0fjm4qjek` 那次点 Allow 后，**URL 没变但页面文本切到**「Enter this code to finish signing in / Copy the code below into Grok Build」**（device-code 流程页面）**，随后成功拿到 CPA token（access_token + refresh_token 真实有效）

### 已尝试的修复
- `xai_oauth.py` `_playwright_drive_login` 的 consent 选择器补了「允许」
- `_playwright_click_first` 改为遍历所有 frame
- else 分支：`wait_for_selector(Allow)` → 普通 click → JS click 兜底 → 检测文本/URL 变化
- 加了大量诊断打印（`[diag]` / `[diag-drive]`）

**结论：点击代码不是根因**。Allow 点不动是**时序/服务端**问题：
- 可能 React 受控按钮需要页面完全就绪后才绑事件，点的时机太早
- 也可能 xAI 对当前出口 IP / 账号做了 consent 提交级风控，POST 被静默拒绝（前端不跳转、不报错）

### 下次继续的建议
1. **先换节点**（住宅 IP），consent 提交级风控可能跟出口 IP 有关。换后 probe_auth 仍会说「干净」（probe 只测 authorize 能否到达，测不出 consent 提交）。
2. 试**有头浏览器** `--headless 0` 跑一个卡住的账号，肉眼看 Allow 点了之后：
   - 是否真的有 device-code 页弹出
   - 是否有短暂错误提示
3. 若确认是时序问题：在 consent 页**等 2-3s 再点**，或循环里持续点直到文本变化（当前代码已加文本变化检测，但可能仍需等页面就绪）。
4. 这 4 个账号 sso cookie 都在 `accounts/` 文件里，sso 本身有效（注册时刚拿到），换干净节点 + 有头浏览器重试很可能成功。

## 待办
- [ ] 转剩余 4 个账号 CPA（换节点 + 有头浏览器重试）
- [ ] 转完后清理 `xai_oauth.py` 里的诊断打印（`[diag]` / `[diag-drive]`）—— 现在留着用于继续调查
- [ ] 把 `convert_new4.py` 扫描路径修复、域名白名单等已确认改动保留

## 代码改动清单（今日）
- `grok_register_ttk.py`：`yyds_pick_domain` 改白名单轮换 ✅ 保留
- `convert_new4.py`：accounts 扫描改为根目录 + `accounts/` 子目录 ✅ 保留
- `xai_oauth.py`：consent 选择器补「允许」+ frame 遍历点击 + 大量诊断 + 文本变化检测 ⚠️ 诊断待清理，逻辑可保留
- `config.json`：`register_count` 当前 = 5（最后跑 5 个时设的）

## 最终更新（人工 consent 调试完成）

- 原剩余 4 个账号现已全部转 CPA 成功，CPA 文件总数从 24 增至 **28**。
- 4 个账号均已检查 JWT 元数据：`referrer=grok-build`，scope 包含
  `conversations:read conversations:write`，base URL 为 `cli-chat-proxy.grok.com/v1`。
- 确认旧 Playwright authorize 默认使用 `referrer=cli-proxy-api` 是权限隐患，已改为
  `grok-build`，并补齐 conversations scopes。
- consent 页的 `/mp/engage`、`/mp/track`、`/mp/flags` 403 属于埋点请求，通常不影响 OAuth；
  真正需要关注的是 `/oauth2/consent`、`/account` 和 `/?_rsc=...`。
- 新增 `manual_convert_cpa.py`：有头浏览器、人工单击 Allow，禁用硬编码 Server Action
  和自动连点，用于快速人工介入。
- 完整交接见 `HANDOFF_CPA_20260718.md`。

### 当前待办

- [x] 转完剩余 4 个账号 CPA
- [x] 修正 `referrer=grok-build` 和 conversations scopes
- [x] 提供人工转换脚本与交接文档
- [ ] 后续稳定后清理 `xai_oauth.py` 的 `[diag]` / `[diag-drive]` 调试输出

## 晚间新增一轮：注册 5 个并继续排查 CPA

### 注册结果

- `accounts/accounts_20260718_223144.txt`：5 个账号，5 成功、0 失败。
- 当前登记账号总数：33。
- 当前 CPA 文件总数：28。
- 新增 5 个均已校验密码和 SSO 字段存在，均尚未生成 CPA：
  - `aaalmst78q@215.singledog.net`
  - `zx2njny2hk@91.txvlogvip.top`
  - `91v2bhr01x@a.bdbdjx.top`
  - `8v58pchxfz@215.singledog.net`
  - `dlttdcd5dq@91.txvlogvip.top`

### CPA 转换实测

- 自动批量转换第一个账号时，consent 提交后出现
  `Sorry, you have been blocked / You are unable to access x.ai`。
- 自动批次随后暴露 Windows GBK 日志编码问题，已在 `convert_new4.py` 中将
  stdout/stderr 重配置为 UTF-8，避免失败日志导致整批退出。
- Cloudflare One Client/WARP 已确认 `Connected / healthy`。
- `127.0.0.1:7890` 当前没有监听，默认代理路径返回
  `ERR_PROXY_CONNECTION_FAILED`。
- `manual_convert_cpa.py` 已增加 `--direct`，可以绕过 7890 直接走系统网络/WARP。
- WARP 直连下 consent GET 可以正常加载，SSO 有效；人工点击“允许”后页面变为
  `Attention Required! | Cloudflare`，OAuth callback 未触发，CPA 未生成。

### 最新结论与待办

- 人工点击不是根因，WARP 共享出口仍被 x.ai 的 consent 提交级风控拦截。
- 不要复用失败的 consent URL；每次必须重新生成 state、PKCE、nonce 和 callback。
- [ ] 断开 WARP，恢复 Clash/Mihomo 的 `127.0.0.1:7890` Mixed Port。
- [ ] 换新的住宅节点，先人工转换 `aaalmst78q@215.singledog.net`。
- [ ] 首个成功后再批量转换另外 4 个。
- [ ] 5 个全部成功后检查 `referrer=grok-build`、conversations scopes 和 CPA 总数 33。
