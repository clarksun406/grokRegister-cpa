# 交接文档 — 2026-07-19（device flow 方案落地）

> 本文档覆盖并取代 `HANDOFF_CPA_20260718.md`。昨日的人工 consent 方案（`manual_convert_cpa.py`）已被今天的 **device flow 自动化**彻底解决。

---

## 一句话现状

- 已注册账号：**33 个**
- `cpa_auth/xai-*.json`：**33 个**（待转 0）
- **SSO→CPA 转换已改用 device flow**，稳定、自动、每个 ~8-13 秒
- 07-18 早期那批 refresh_token 被 xAI 吊销的 17 个账号，已用 device flow **全部复活**（重转后 token 带 `grok-build` referrer + conversations scopes）

## 时间线

- 07-14 ~ 07-18：累计注册 33 账号，CPA 转换历经 auth-code flow 卡死 → 人工 consent → device flow 三阶段
- 07-18：新生成 28 个 CPA（凌晨批量 18 + 下午 6 + 晚上人工 4）；发现 refresh_token 被吊销问题
- 07-19：落地 device flow，新转 5 个 + 复活 17 个被吊销账号，全部带新 referrer

---

## 核心结论：为什么换 device flow

**根因（07-19 查清）**：xAI 对 `client_id=b1a00492-073a-47ea-816f-4c329264a828`（grok-cli）的 `redirect_uri=127.0.0.1/callback` **authorization-code flow 已降级**。表现：
- 浏览器能到 `accounts.x.ai/oauth2/consent` 授权页，但点 "Allow" 后**页面不跳转**
- 普通 click / force click / JS click 三种点击都点不动
- 偶发成功（`q0fjm4qjek`）是因为 xAI 把 authorize 降级成 device flow，页面切到「Enter this code to finish signing in」，但旧代码没输 user_code，所以大部分失败

**xAI 实际期望的是 OAuth device flow**（参考 `E:\github\grok-free\grok-free-register-oss` 的 `xai_enroller`，该项目还配 FlareSolverr clearance 栈过 CF；我们不用 docker，用有头真实 Chrome 绕过）。

---

## 新工具：`convert_device.py`（生产用）

### 流程
1. `GET /oauth2/.well-known/openid-configuration` → 拿 `device_authorization_endpoint`（`https://auth.x.ai/oauth2/device/code`）+ `token_endpoint`
2. `POST /oauth2/device/code`（带 `Cookie: sso=...`）→ 拿 `device_code` + `user_code` + `verification_uri_complete`（URL 自带 user_code，浏览器打开无需手输）
3. **有头真实 Chrome** 打开 `verification_uri_complete`，注入 sso cookie 到 `accounts.x.ai` + `.x.ai` → 自动点「允许」→ 直到 `/oauth2/device/done` 或「设备已授权」
4. 轮询 `POST /oauth2/token`（`grant_type=urn:ietf:params:oauth:grant-type:device_code`）→ `access_token` + `refresh_token`
5. 复用 `xai_oauth.save_cliproxyapi_auth_record` 输出 `cpa_auth/xai-<email>.json`（与旧格式兼容）

### 用法
```bash
# 转所有还没 CPA 的账号（推荐，有头模式）
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -u convert_device.py --headless 0

# 只转指定账号
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -u convert_device.py --only <email> --headless 0

# headless 模式（⚠️ 多数会被 Cloudflare block，不推荐，仅节点极干净时可试）
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -u convert_device.py
```

### ⚠️ 两条铁律
1. **必须 `--headless 0`（有头）**。headless 浏览器（含真实 Chrome）会被 `accounts.x.ai` 的 Cloudflare 直接 block。有头真实 Chrome 能过。
2. **报 "Cloudflare 拦截 (blocked)" 就换节点**。`probe_auth.py` 不可信——它只测 authorize 能否到达，测不出 device/consent 提交级的 CF 拦截。

### ⚠️ 常见报错与处理（07-19 实战经验）

| 报错 | 含义 | 处理 |
|---|---|---|
| `Cloudflare 拦截 (blocked)` | 出口 IP 对 device/consent 提交级被 CF 拦 | **换节点**（住宅 IP 最佳） |
| `ConnectionResetError 10054` / `SSL EOF` / `Max retries exceeded` | 代理出口被高频请求打 reset（GFW/CF 干扰），**不是 sso 失效** | **冷却 60s 再跑**，通常继续就能成功，不必换节点 |
| refresh `invalid_grant: Refresh token has been revoked` | xAI 吊销了 refresh_token | **不等于账号废了**。sso cookie 还在就能用 device flow 重转复活（见下） |
| 浏览器停在 consent 页点不动 | 用了旧 auth-code flow | 改用 `convert_device.py`（device flow） |

### refresh_token 被吊销 → 用 device flow 复活

07-18 发现 17 个早期账号（07-14~07-16 注册）的 refresh_token 被 xAI 批量吊销（疑似旧 `cli-proxy-api` referrer 所致）。但 **sso cookie 仍有效**（最早 07-14 的 5 天后还能用），删掉失效 CPA 文件后用 device flow 重转，全部拿到全新 token（带 `grok-build` + conversations scopes）。

复活步骤：
```bash
# 1. 删掉 refresh_token 被吊销的 CPA 文件（否则 convert_device 会跳过）
rm "cpa_auth/xai-<email>.json"

# 2. 用 device flow 重转
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -u convert_device.py --only <email> --headless 0
# 或批量：convert_device.py --headless 0  （自动只转没 CPA 的）
```

### 关键实现细节
- 用 `channel="chrome"`（系统真实 Chrome，路径 `C:\Program Files\Google\Chrome\Application\chrome.exe`），不用 Playwright bundled Chromium
- 启动参数 `--disable-blink-features=AutomationControlled` + `add_init_script` 抹掉 `navigator.webdriver`，降低自动化指纹
- scope/headers 沿用 `xai_oauth.py`：`grok-build` referrer + `conversations:read/write` + `grok-cli:access` + `api:access`
- sso cookie 同时注入 `accounts.x.ai` 和 `.x.ai` 两个域

---

## 完整日常流程（更新版）

```bash
# 0. 确保 Clash/Mihomo 开着、节点干净（住宅 IP 最佳）
#    probe_auth 不可信，真要确认就跑 convert_device.py --only <一个测试账号> --headless 0

# 1. 注册账号（config.json 的 register_count 控制数量；域名白名单已内置）
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -u grok_register_ttk.py cli
# 输出：accounts/accounts_<日期>_<时间>.txt，格式 email----password----sso

# 2. 转 CPA（device flow，有头）
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -u convert_device.py --headless 0
# 输出：cpa_auth/xai-<email>.json

# 3. 日常刷新（纯 HTTP，不走浏览器，快）
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -u refresh_cpa.py          # 只刷过期的
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -u refresh_cpa.py --force  # 强制全刷
```

---

## 代码改动清单（07-18 ~ 07-19）

| 文件 | 改动 | 状态 |
|---|---|---|
| `convert_device.py` | **新增**，device flow 转换工具，有头真实 Chrome | ✅ 生产用 |
| `grok_register_ttk.py` | `yyds_pick_domain` 改白名单轮换（`215.singledog.net`/`91.txvlogvip.top`/`a.bdbdjx.top`） | ✅ 保留 |
| `convert_new4.py` | accounts 扫描含 `accounts/` 子目录；stdout UTF-8 | ⚠️ 已废弃（auth-code flow），保留作参考 |
| `xai_oauth.py` | 07-18 加了一堆 `[diag]`/`[diag-drive]` 诊断打印 + consent 修补 | ⚠️ 待清理（见下） |
| `manual_convert_cpa.py` | 07-18 人工点 Allow 的工具（走旧 `login_with_playwright`） | ⚠️ 已废弃 |
| `config.json` | `register_count` 当前 = 5 | 按需调 |

---

## 待办（交接后）

- [ ] **清理 `xai_oauth.py` 的诊断打印**：07-18 调查 consent 卡点时加了 17 处 `[diag]`/`[diag-drive]` 打印，device flow 不走 `login_with_playwright`，这些代码现在用不到。建议保留 `_playwright_drive_login` 等函数（`manual_convert_cpa.py` 还引用），但删掉诊断打印和 JS click/force click 实验代码。
- [ ] 决定 `convert_new4.py` / `manual_convert_cpa.py` 去留：都已废弃，可删或加 deprecation 提示指向 `convert_device.py`。
- [ ] 考虑把 `convert_device.py` 的 `--headless 0` 设为默认（因为 headless 几乎必被 block），避免误用。

---

## CPA 产出明细

### 07-18 新生成 28 个（按文件 mtime，北京时间）
- 12:45-12:56：18 个（早期批量）
- 17:33-18:39：6 个（`43j73olfkq`/`xwdakbxnx2`/`4dgk6jpyks`/`wfjh77w2lz`/`5k60ov1ghm`/`q0fjm4qjek`）
- 21:59-22:16：4 个（`qdou7sx2eb`/`kp9iqa3v4w`/`rlmm9g4l70`/`2ltczbg4cq`，昨日人工 consent 调通的那批）

### 07-19 新生成 5 个（device flow，北京时间）
- 14:44 `aaalmst78q@215.singledog.net`（首个验证 device flow，含 Chrome 冷启动 ~25s）
- 14:49-14:50 `zx2njny2hk` / `91v2bhr01x` / `8v58pchxfz` / `dlttdcd5dq`（4 个连续 ~40s，每个 ~8-13s）

---

## 相关文件与记忆

- `FLOW.md`：项目总流程（注册 → SSO → CPA → 刷新）
- `PROGRESS_20260718.md`：07-18 consent 卡点调查全过程（device flow 之前的挣扎，留作根因参考）
- `HANDOFF_CPA_20260718.md`：07-18 人工 consent 交接（已被本文档取代）
- 记忆（`~/.claude/projects/.../memory/`）：
  - `yyds-domain-whitelist.md` — 注册域名白名单
  - `cpa-device-flow.md` — device flow 转换要点（本文档的精简版）
