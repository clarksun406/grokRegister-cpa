# Chat 403 账号排查 — 2026-07-19

## 现象

33 个 CPA 账号里，**10 个**调 `cli-chat-proxy.grok.com/v1/chat/completions` 返回 403：

```json
{"code":"permission-denied",
 "error":"Access to the chat endpoint is denied. Please ensure you're using the correct credentials. If you believe this is a mistake, please log into console.x.ai and update the permissions, or contact support."}
```

其余 23 个正常返回 200（model=`grok-4.5-build-free`，带 `x-zero-data-retention: true`、`x-ratelimit-limit-requests: 21`）。

## 受影响账号（10 个，全部 07-17 23:42 之后注册）

| 账号 | 注册文件 | token referrer |
|---|---|---|
| `q0f5lwdubq@91.txvlogvip.top` | accounts_20260717_234248.txt | 无 |
| `awq9xz6f0z@215.singledog.net` | accounts_20260717_235309.txt | 无 |
| `xwdakbxnx2@91.txvlogvip.top` | accounts_20260718_170643.txt | cli-proxy-api |
| `5k60ov1ghm@a.bdbdjx.top` | accounts_20260718_171550.txt | 无 |
| `wfjh77w2lz@91.txvlogvip.top` | accounts_20260718_171550.txt | cli-proxy-api |
| `kp9iqa3v4w@91.txvlogvip.top` | accounts_20260718_172213.txt | grok-build |
| `aaalmst78q@215.singledog.net` | accounts_20260718_223144.txt | 无 |
| `zx2njny2hk@91.txvlogvip.top` | accounts_20260718_223144.txt | 无 |
| `91v2bhr01x@a.bdbdjx.top` | accounts_20260718_223144.txt | 无 |
| `dlttdcd5dq@91.txvlogvip.top` | accounts_20260718_223144.txt | 无 |

> 上游告警里看到的 5 个（mehexs12pe/5k60ov1ghm/91v2bhr01x/aaalmst78q/awq9xz6f0z）是这 10 个中被实际调用到的子集。注意 `mehexs12pe` 在批量测时是 200，不在 403 列表。

**关键时间线**：分界线在 **2026-07-17 23:42**。此时间点之前注册的账号全可用；之后注册的（除一个例外 `qdou7sx2eb`）全 403。强烈指向 **xAI 在该时间点改了新账号默认的 Grok Build free 额度策略**。

## 已排除的原因（都不是根因）

1. **不是 token 过期** —— 10 个 token 都有效（exp 在当天 21:xx，models 接口 200）
2. **不是 scope 缺失** —— `5k60ov1ghm` 用 device flow 重转补齐 `conversations:read/write` 后仍 403
3. **不是 referrer** —— 403 和 200 的账号里都混有 grok-build / cli-proxy-api / 无 referrer
4. **不是 CPA 文件损坏** —— token 能调通 models 接口
5. **不是 refresh_token 吊销** —— 那是另一个独立问题（17 个早期账号已用 device flow 复活）

## 已尝试且失败的修复

### 1. console.x.ai 创建 team + API key
- 用 `sso-rw` 注入（sso-rw 值 = sso，注册脚本里两者同值）→ **CreateTeam 成功**、**CreateApiKey 成功**，拿到 console API key
- 但 chat 仍 403。原因：console 的 team/API key 是**付费 API**通道（`api.x.ai`，报 "no credits or licenses"），跟 Grok Build free 通道（`cli-chat-proxy.grok.com`）是两套独立系统
- **结论**：console team 创建不解决 chat 权限

### 2. 注入 sso 打开 grok.com 激活
- grok.com 页面显示 "Grok Build / Try Free"（说明账号没激活免费试用）
- 但 `grok.com` 域名的 Cloudflare 挑战比 `accounts.x.ai` 严得多，机房 IP 出口过不去（"正在进行安全验证" / "Just a moment..." 卡死）
- 换节点仍过不去
- **结论**：自动化注入 sso 激活的路被 grok.com CF 挑战卡死

## 根因（结论）

- **能用的账号** = 在 grok.com 激活过 Grok Build 免费试用，服务端有 free 额度（`grok-4.5-build-free` model、`x-zero-data-retention`）
- **不能用的 10 个** = 07-17 23:42 后注册、没在 grok.com 激活，服务端没 free 额度
- 这是**账号服务端状态**，不是 token/cookie 问题，无法从 CPA 文件层面修复

## 尚未验证的疑点

- **07-17 23:42 之后新注册的账号是不是默认都没 free 额度？** 还没注册新账号验证。如果 xAI 现在对所有新账号都不给默认 free 额度，那即使手工激活这 10 个、再注册新账号也照样 403。
- `qdou7sx2eb`（07-18 注册）是例外能用，原因未知——可能注册时偶然完成了某个激活步骤。

## ✅ 已验证：新账号策略正常（2026-07-19 18:44~18:56）

注册 5 个全新账号 → device flow 转 CPA → 测 chat，**5/5 全部 200**，model 均为 `grok-4.5-build-free`：

| 新账号 | chat | model |
|---|---|---|
| `3urhm60ia2@215.singledog.net` | 200 | grok-4.5-build-free |
| `zge55loqw4@215.singledog.net` | 200 | grok-4.5-build-free |
| `dtrhwrejbq@91.txvlogvip.top` | 200 | grok-4.5-build-free |
| `k2t8sedreu@a.bdbdjx.top` | 200 | grok-4.5-build-free |
| `93glvafgw3@215.singledog.net` | 200 | grok-4.5-build-free |

**结论**：xAI **没有**收紧新账号 free 额度策略。那 10 个 403 是**该批次特定账号的孤立问题**（07-17 23:42 ~ 07-18 22:31 注册的被 xAI 临时风控/未激活 free 额度），与注册时间政策无关。

## 最终结论与方案

- **新账号照常可用** → 不必手工激活那 10 个废号，**直接注册新账号补齐更省事**
- 那 10 个 403 账号的处理：搁置（或后续有住宅 IP 再手工激活 grok.com 试）
- 当前可用账号 = 23 老 + 5 新 = **28 个**；补齐到目标数后即可结束

## ✅ 最终补齐（2026-07-19 19:20）

放弃 10 个废号，再注册 10 个新账号补齐：
- 注册 10 个，**10 成功 0 失败**
- device flow 转 10 个 CPA，**10 成功 0 失败**
- 批量测 chat，**10/10 全部 200**（`grok-4.5-build-free`）

新补齐的 10 个账号：
`pgy4rl7bp2@215.singledog.net`、`mor5xo9ncs@91.txvlogvip.top`、`k4xk2u14vz@a.bdbdjx.top`、`2anhbaxqd4@215.singledog.net`、`kco5k6m8t1@91.txvlogvip.top`、`1x92tlqhwq@a.bdbdjx.top`、`ztngd6lx6y@215.singledog.net`、`kswqjx63h2@91.txvlogvip.top`、`2tnyaej7er@a.bdbdjx.top`、`i2ejrugnjz@215.singledog.net`

## 最终账号清单（48 CPA）

- **可用 38**（23 老 + 5 新[07-19 下午] + 10 新[07-19 晚补齐]）
- **403 废号 10**（07-17 23:42 ~ 07-18 22:31 注册的那批，搁置）

### 目录划分

为避免上游网关误调废号触发告警，废号已挪出主目录：

- `cpa_auth/` — **38 个可用** CPA（chat 200）
- `cpa_auth_403/` — **10 个废号** CPA（chat 403，token 本身有效能调 models，但账号无 Grok Build free 额度）

废号的 sso 仍在 `accounts/accounts_*.txt` 里。若日后用住宅 IP 在 `grok.com` 手工激活 free 试用成功，把对应文件从 `cpa_auth_403/` 挪回 `cpa_auth/` 即可。


## 待选方案（更新）

| 方案 | 说明 | 状态 |
|---|---|---|
| A. 手工激活 10 个废号 | 住宅 IP 逐个登录 grok.com 激活 | 搁置（费事，新注册更划算） |
| B. 住宅节点脚本批量激活 | 依赖能过 grok.com CF 的节点 | 搁置 |
| **C. 放弃 10 个废号，注册新账号补齐** | 新账号策略正常，已验证 5/5 可用 | ✅ 采用 |

## 相关代码

- `probe_console.py` — 注入 sso/sso-rw 打开 console.x.ai/grok.com 的诊断脚本（CreateTeam 已验证可行，grok.com CF 过不去）

